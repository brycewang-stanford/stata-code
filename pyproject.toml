[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "stata_code"
version = "0.2.0"
description = "Agent-native Stata bridge — one core, multiple frontends (MCP, Jupyter, VSCode)"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "Bryce Wang", email = "brycewang@stanford.edu" }
]
keywords = ["stata", "jupyter", "vscode", "mcp", "pystata", "causal-inference"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Mathematics",
    "Topic :: Software Development :: Libraries :: Python Modules",
]

dependencies = [
    "pydantic>=2.0",
    # `pystata` ships with Stata 17+ at /Applications/Stata/utilities/pystata
    # (macOS) or equivalent — it is NOT on PyPI. Our `_runtime` module locates
    # and adds it to sys.path on init, so we deliberately do not declare it
    # as a runtime dependency. Users without Stata 17+ will get a clear
    # `PystataNotAvailable` error.
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=4.0",
    "ruff>=0.4.0",
    "mypy>=1.8",
]
mcp = ["mcp>=1.0"]
kernel = ["ipykernel>=6.0"]
all = ["stata_code[mcp,kernel]"]

[project.scripts]
stata-code-kernel = "stata_code.kernel.kernel:run_main"
stata-code-mcp = "stata_code.mcp.server:run_main"

[project.entry-points."jupyter kernelspec"]
stata = "stata_code.kernel:install_kernel"

[project.urls]
Homepage = "https://github.com/brycewang-stanford/stata_code"
Repository = "https://github.com/brycewang-stanford/stata_code"
Issues = "https://github.com/brycewang-stanford/stata_code/issues"

[tool.hatch.build.targets.wheel]
packages = ["stata_code"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_ignores = true
disallow_untyped_defs = true

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
